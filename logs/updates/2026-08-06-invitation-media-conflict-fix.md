# 2026-08-06｜邀评媒体去重写入修复

## 本次目标

修复邀请评价页面上传图片或视频后预览消失、无法提交的问题。

## 修改范围

- `src/worker.ts`
- `logs/bugs/2026-08-06-invitation-media-conflict-predicate.md`

## 调整内容

- 使媒体元数据写入的去重条件与 PostgreSQL 部分唯一索引保持一致。
- 继续保留按邀评任务和文件哈希去重的机制，避免浏览器重试产生重复媒体记录。

## 影响范围

- 新上传的邀评图片和视频可以正常写入媒体元数据并返回前端。
- 已有评论、审核、主题前台展示及测试邀评流程不改变。

## 自检

- `npm run typecheck` 通过。
- `npm run build` 通过。
- `npm test -- --run` 通过：9 个测试文件、27 项测试。
- 已通过真实邀评媒体接口上传本地 PNG，并在验证后删除该测试记录。

## 遗留事项

- 修复前因写入失败产生的 Shopify Files 孤儿文件不会自动清理；后续可单独做后台清理工具。
